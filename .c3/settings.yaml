# C3 Project Settings
c3-version: 3

# Diagram preferences
diagrams:
  tool: mermaid

# Layer-specific guidance
layers:
  context:
    actors: [Developer/Team, Internal Tool, Public User]
  container:
    archetypes: [HTTP Service]
  component:
    patterns: [atoms, flows, tags]

# Handoff steps after design work
handoff:
  - Summarize changes to user
  - List affected code locations
  - Confirm completion
